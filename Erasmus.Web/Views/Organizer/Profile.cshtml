@model Erasmus.Domain.DTO.OrganizerProfileDto
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<head>
    <link rel="stylesheet" href="~/css/organizer-profile.css" />
    
</head>

<div class="container">
    
    <div class="row main-row">
        <div class="col-3 left">
                <div class="row profile-picture profile-picture">
                        @if(Model.ProfilePicture != null)
                        {
                            <img class="shape" src="~/images/@Model.ProfilePicture.PathOnDisk"/>
                        }
                </div>
                <div class="row">
                    <a asp-controller="Organizer" asp-action="EditProfilePhoto" asp-route-organizerId="@Model.OrganizerId" class="btn button-upload btn-edit btn-outline-success">Upload a profile picture</a>
                </div>
        </div>
        <div class="col-9 right">
                <label form-control>First Name</label>
                @if(!string.IsNullOrEmpty(Model.FirstName))
                {
                    <p>@Model.FirstName</p>
                }
                else{
                    <p>First Name is not inserted</p>
                }
                
                 <label form-control>Last Name</label>
                @if(!string.IsNullOrEmpty(Model.LastName))
                {
                    <p>@Model.LastName</p>
                }
                else{
                    <p>Last Name not inserted</p>
                }

                <hr />

                 <label form-control>Organization Name</label>
                @if(!string.IsNullOrEmpty(Model.OrganizationName))
                {
                    <p>@Model.OrganizationName</p>
                }
                else
                {
                    <p>Organization Name not inserted</p>
                }

                 <label form-control>Organization Contact</label>
                @if(!string.IsNullOrEmpty(Model.OrganizationContact))
                {
                    <p>@Model.OrganizationContact</p>   
                } 
                else
                {
                    <p>Organization Contact not inserted</p>
                }
                <a asp-controller="Organizer" asp-action="Edit" asp-route-id="@Model.OrganizerId" class="btn btn-outline-success btn-edit">Edit Profile</a>
        </div>
    </div>
    
</div>
